// MDX content strings keyed by slug. Keep tiny for demo; replace with real MDX files later.
const postsContent = {
  welcome: `# Thanks for stopping by! I'm just checking if this works. 😋`,
};

export default postsContent;